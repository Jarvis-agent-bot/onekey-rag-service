# Frontend-Admin 页面设计文档

> 本文档旨在为 AI 页面重新设计提供完整的上下文信息，包含每个页面的功能定位、UI 结构、用户流程和关键交互。

---

## 目录

1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [整体架构](#整体架构)
4. [页面详情](#页面详情)
   - [登录页 (LoginPage)](#登录页-loginpage)
   - [仪表盘 (DashboardPage)](#仪表盘-dashboardpage)
   - [知识库列表 (KbsPage)](#知识库列表-kbspage)
   - [知识库详情 (KbDetailPage)](#知识库详情-kbdetailpage)
   - [应用列表 (AppsPage)](#应用列表-appspage)
   - [应用详情 (AppDetailPage)](#应用详情-appdetailpage)
   - [用户反馈 (FeedbackPage)](#用户反馈-feedbackpage)
   - [质量监控 (QualityPage)](#质量监控-qualitypage)
   - [可观测性 (ObservabilityPage)](#可观测性-observabilitypage)
   - [内容列表 (PagesPage)](#内容列表-pagespage)
   - [内容详情 (PageDetailPage)](#内容详情-pagedetailpage)
   - [任务中心 (JobsPage)](#任务中心-jobspage)
   - [任务详情 (JobDetailPage)](#任务详情-jobdetailpage)
   - [审计日志 (AuditPage)](#审计日志-auditpage)
   - [系统设置 (SettingsPage)](#系统设置-settingspage)
   - [检索事件详情 (RetrievalEventDetailPage)](#检索事件详情-retrievaleventdetailpage)
5. [用户流程图](#用户流程图)
6. [设计改进建议](#设计改进建议)

---

## 项目概述

**OneKey RAG Service Admin** 是一个 RAG（检索增强生成）服务的管理后台，主要功能包括：

- **知识库管理**：创建、配置和管理知识库，设置数据源
- **应用管理**：创建和配置 RAG 应用，关联知识库
- **内容管理**：管理抓取的页面内容、chunks
- **任务管理**：监控抓取和索引任务
- **可观测性**：监控请求、性能指标、质量指标
- **用户反馈**：收集和分类用户反馈
- **系统设置**：配置模型、检查健康状态

---

## 技术栈

| 类别 | 技术 |
|------|------|
| 框架 | React 18 + TypeScript |
| 路由 | React Router v6 |
| 状态管理 | TanStack Query (React Query) |
| 样式 | Tailwind CSS |
| UI 组件 | 自定义组件库 (shadcn/ui 风格) |
| 图表 | Recharts |
| 通知 | Sonner (Toast) |
| 图标 | Lucide React |

---

## 整体架构

### 路由结构

```
/login                          → 登录页
/                               → AdminLayout (带侧边栏的主布局)
  ├── /                         → 仪表盘
  ├── /kbs                      → 知识库列表
  ├── /kbs/:kbId                → 知识库详情
  ├── /apps                     → 应用列表
  ├── /apps/:appId              → 应用详情
  ├── /feedback                 → 用户反馈
  ├── /quality                  → 质量监控
  ├── /observability            → 可观测性
  ├── /observability/:eventId   → 检索事件详情
  ├── /pages                    → 内容列表
  ├── /pages/:pageId            → 内容详情
  ├── /jobs                     → 任务中心
  ├── /jobs/:jobId              → 任务详情
  ├── /audit                    → 审计日志
  └── /settings                 → 系统设置
```

### 主布局 (AdminLayout)

**结构**：
```
┌─────────────────────────────────────────────────────────────┐
│ Header: Logo + Workspace Selector                           │
├─────────────┬───────────────────────────────────────────────┤
│             │                                               │
│  Sidebar    │           Main Content Area                   │
│  Navigation │           (Outlet)                            │
│             │                                               │
│  - 仪表盘    │                                               │
│  - 知识库    │                                               │
│  - 应用      │                                               │
│  - 反馈      │                                               │
│  - 质量      │                                               │
│  - 可观测性  │                                               │
│  - 内容      │                                               │
│  - 任务      │                                               │
│  - 审计      │                                               │
│  - 设置      │                                               │
│             │                                               │
├─────────────┴───────────────────────────────────────────────┤
│ Footer: Version + Links                                     │
└─────────────────────────────────────────────────────────────┘
```

**侧边栏分组**：
1. **Overview**: 仪表盘
2. **Core**: 知识库、应用
3. **Insights**: 反馈、质量、可观测性
4. **Content**: 内容、任务
5. **System**: 审计、设置

---

## 页面详情

### 登录页 (LoginPage)

**功能定位**：用户身份认证入口

**UI 结构**：
```
┌─────────────────────────────────────────┐
│                                         │
│          OneKey RAG Service             │
│                Logo                     │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  用户名输入框                    │   │
│  └─────────────────────────────────┘   │
│  ┌─────────────────────────────────┐   │
│  │  密码输入框                      │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │          登录按钮                │   │
│  └─────────────────────────────────┘   │
│                                         │
│        错误提示区域（条件显示）         │
│                                         │
└─────────────────────────────────────────┘
```

**用户流程**：
1. 输入用户名和密码
2. 点击登录按钮
3. 成功 → 跳转到仪表盘
4. 失败 → 显示错误信息

**关键交互**：
- 表单验证
- 加载状态指示
- 错误消息展示

---

### 仪表盘 (DashboardPage)

**功能定位**：系统概览入口，展示关键指标和快捷入口

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Hero Section                                               │
│  - 标题：RAG Service Admin                                  │
│  - 副标题：当前 Workspace 信息                               │
│  - 快捷操作：新建知识库、新建应用                            │
├─────────────────────────────────────────────────────────────┤
│  Statistics Cards (Grid 2x2)                                │
│  ┌──────────────────┐ ┌──────────────────┐                 │
│  │ 知识库数量        │ │ 应用数量          │                 │
│  │ 数字 + 图标      │ │ 数字 + 图标       │                 │
│  └──────────────────┘ └──────────────────┘                 │
│  ┌──────────────────┐ ┌──────────────────┐                 │
│  │ 页面数量         │ │ 任务数量          │                 │
│  │ 数字 + 图标      │ │ 数字 + 图标       │                 │
│  └──────────────────┘ └──────────────────┘                 │
├─────────────────────────────────────────────────────────────┤
│  Quick Links                                                │
│  - 查看所有知识库                                            │
│  - 查看所有应用                                              │
│  - 查看用户反馈                                              │
│  - 系统设置                                                  │
└─────────────────────────────────────────────────────────────┘
```

**数据展示**：
- 知识库总数、应用总数
- 页面总数、运行中任务数
- 最近活动（可选）

**用户流程**：
1. 登录后默认进入
2. 查看系统概况
3. 通过快捷链接进入各模块

---

### 知识库列表 (KbsPage)

**功能定位**：知识库的 CRUD 管理中心

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 标题：知识库管理                                          │
│  - 描述：创建和管理知识库                                    │
│  - 操作按钮：新建知识库                                      │
├─────────────────────────────────────────────────────────────┤
│  Filter Section (Card)                                      │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 搜索框 (KB ID / 名称)  │  状态筛选  │  刷新  │  清空  │   │
│  └─────────────────────────────────────────────────────┘   │
│  Filter Chips (当前激活的筛选条件)                          │
├─────────────────────────────────────────────────────────────┤
│  List Section (Card)                                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Table Header                                         │   │
│  │ ID | 名称 | 状态 | 数据源数 | 页面数 | 创建时间 | 操作 │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ Row 1: kb_xxx | 产品文档 | active | 3 | 150 | ... | ▶  │   │
│  │ Row 2: kb_yyy | 帮助中心 | active | 2 | 80  | ... | ▶  │   │
│  │ ...                                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│  Pagination                                                 │
└─────────────────────────────────────────────────────────────┘
```

**关键功能**：
- 分页列表展示
- 搜索和筛选
- 新建知识库（Dialog）
- 点击行进入详情

**用户流程**：
1. 浏览知识库列表
2. 使用筛选缩小范围
3. 点击进入详情或新建

---

### 知识库详情 (KbDetailPage)

**功能定位**：单个知识库的完整管理界面

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 面包屑导航：知识库 > kb_xxx                               │
│  - 知识库名称 + 状态 Badge                                  │
│  - 操作按钮：编辑 | 删除                                     │
├─────────────────────────────────────────────────────────────┤
│  Tab Navigation                                             │
│  [ 概览 ] [ 数据源 ] [ 内容 ] [ Chunks ] [ 任务 ]           │
├─────────────────────────────────────────────────────────────┤
│  Tab Content (根据选中 Tab 变化)                             │
│                                                             │
│  【概览 Tab】                                                │
│  ┌──────────────────┐ ┌──────────────────┐                 │
│  │ 基本信息          │ │ 统计数据          │                 │
│  │ - ID             │ │ - 数据源数量      │                 │
│  │ - 名称           │ │ - 页面数量        │                 │
│  │ - 描述           │ │ - Chunk 数量      │                 │
│  │ - 创建时间       │ │ - 索引覆盖率      │                 │
│  └──────────────────┘ └──────────────────┘                 │
│                                                             │
│  【数据源 Tab】                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ + 添加数据源                                         │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ Source 1: sitemap | https://... | 状态 | 操作        │   │
│  │ Source 2: single  | https://... | 状态 | 操作        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  【内容 Tab】→ 嵌入 PagesPage (filtered by kb_id)           │
│  【Chunks Tab】→ Chunks 列表                                │
│  【任务 Tab】→ 嵌入 JobsPage (filtered by kb_id)            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**数据源类型**：
- `sitemap`: 站点地图抓取
- `single`: 单页面抓取
- `api`: API 导入

**关键操作**：
- 添加/编辑/删除数据源
- 触发抓取任务
- 触发索引任务
- 查看关联内容和任务

**用户流程**：
1. 从列表进入详情
2. 查看概览信息
3. 管理数据源
4. 监控任务状态
5. 浏览和管理内容

---

### 应用列表 (AppsPage)

**功能定位**：RAG 应用的管理中心

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 标题：应用管理                                            │
│  - 操作按钮：新建应用                                        │
├─────────────────────────────────────────────────────────────┤
│  Filter Section                                             │
│  搜索框 | 状态筛选 | 刷新 | 清空                             │
├─────────────────────────────────────────────────────────────┤
│  List Section                                               │
│  Table: ID | 名称 | 状态 | 关联KB数 | 创建时间 | 操作        │
│  Pagination                                                 │
└─────────────────────────────────────────────────────────────┘
```

**关键功能**：
- 应用 CRUD
- 搜索筛选
- 分页

---

### 应用详情 (AppDetailPage)

**功能定位**：单个应用的配置和测试界面

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 面包屑：应用 > app_xxx                                   │
│  - 应用名称 + 状态                                          │
│  - 操作：编辑 | 删除                                         │
├─────────────────────────────────────────────────────────────┤
│  Tab Navigation                                             │
│  [ 配置 ] [ 知识库 ] [ 测试 ] [ 统计 ]                       │
├─────────────────────────────────────────────────────────────┤
│  【配置 Tab】                                                │
│  - 基本信息（ID、名称、描述）                                │
│  - 模型配置（LLM、Embedding）                               │
│  - 检索配置（Top-K、阈值）                                  │
│  - Prompt 模板                                              │
│                                                             │
│  【知识库 Tab】                                              │
│  - 已关联知识库列表                                          │
│  - 添加/移除关联                                             │
│                                                             │
│  【测试 Tab】                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Chat Interface                                       │   │
│  │ ┌─────────────────────────────────────────────────┐ │   │
│  │ │ 消息历史区域                                     │ │   │
│  │ │ User: 什么是...                                  │ │   │
│  │ │ Assistant: 根据知识库...                         │ │   │
│  │ └─────────────────────────────────────────────────┘ │   │
│  │ ┌─────────────────────────────────────────────────┐ │   │
│  │ │ 输入框                              │  发送按钮  │ │   │
│  │ └─────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  【统计 Tab】                                                │
│  - 调用次数、Token 使用量、平均延迟                          │
└─────────────────────────────────────────────────────────────┘
```

**关键功能**：
- 配置管理（模型、检索参数、Prompt）
- 知识库关联
- 实时测试对话
- 使用统计

---

### 用户反馈 (FeedbackPage)

**功能定位**：收集和分类用户对 RAG 回答的反馈

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 标题：用户反馈                                            │
│  - 描述：查看和分类用户反馈                                  │
├─────────────────────────────────────────────────────────────┤
│  Filter Section                                             │
│  应用ID | 评分筛选 | 原因筛选 | 时间范围 | 搜索              │
├─────────────────────────────────────────────────────────────┤
│  Statistics Cards                                           │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐              │
│  │ 总反馈数    │ │ 好评数     │ │ 差评数      │              │
│  │   150      │ │   120      │ │   30       │              │
│  └────────────┘ └────────────┘ └────────────┘              │
├─────────────────────────────────────────────────────────────┤
│  Feedback List                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Feedback Card                                        │   │
│  │ ┌─────────────────────────────────────────────────┐ │   │
│  │ │ 👍/👎 | 原因: 信息不准确 | 时间: 2024-01-15      │ │   │
│  │ │ 问题: 什么是...                                  │ │   │
│  │ │ 回答: 根据知识库...                              │ │   │
│  │ │ 用户评论: 回答和问题不相关                       │ │   │
│  │ │ [标记已处理] [查看详情]                          │ │   │
│  │ └─────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────┘   │
│  Pagination                                                 │
└─────────────────────────────────────────────────────────────┘
```

**反馈类型**：
- 好评 (thumbs_up)
- 差评 (thumbs_down)

**差评原因分类**：
- 信息不准确
- 回答不相关
- 回答不完整
- 其他

**关键操作**：
- 筛选和搜索
- 标记处理状态
- 查看对话上下文

---

### 质量监控 (QualityPage)

**功能定位**：RAG 系统质量指标的可视化监控

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 标题：质量监控                                            │
│  - 时间范围选择器：24h | 7d | 30d                            │
├─────────────────────────────────────────────────────────────┤
│  Overview Cards (Grid)                                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 请求总数  │ │ 错误率   │ │ 命中率   │ │ 平均延迟  │       │
│  │  12,345  │ │  0.5%   │ │  85%    │ │  1.2s   │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
├─────────────────────────────────────────────────────────────┤
│  Charts Section                                             │
│                                                             │
│  【延迟分布图】                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Area Chart: P50 / P90 / P99 延迟趋势                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  【Token 使用量】                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Stacked Bar: 按模型分类的 Token 消耗                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  【Rerank 效果】                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Before/After 对比图                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**关键指标**：
- 请求量趋势
- 错误率
- 检索命中率
- 延迟分位数 (P50/P90/P99)
- Token 使用量（按模型）
- Rerank 效果对比

---

### 可观测性 (ObservabilityPage)

**功能定位**：请求级别的追踪和系统资源监控

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 标题：可观测性                                            │
│  - 时间范围 | 刷新                                           │
├─────────────────────────────────────────────────────────────┤
│  Tab Navigation                                             │
│  [ 请求追踪 ] [ 资源监控 ]                                   │
├─────────────────────────────────────────────────────────────┤
│  【请求追踪 Tab】                                            │
│  Filter: 应用ID | 状态 | 搜索                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Table                                                │   │
│  │ Event ID | 应用 | 状态 | 延迟 | 时间 | 操作           │   │
│  │ evt_xxx  | app1 | success | 1.2s | ... | 详情        │   │
│  └─────────────────────────────────────────────────────┘   │
│  Pagination                                                 │
│                                                             │
│  【资源监控 Tab】                                            │
│  ┌────────────────────────┐ ┌────────────────────────┐     │
│  │ CPU Usage Chart        │ │ Memory Usage Chart     │     │
│  │ Line Chart             │ │ Line Chart             │     │
│  └────────────────────────┘ └────────────────────────┘     │
│  ┌────────────────────────┐ ┌────────────────────────┐     │
│  │ Network I/O Chart      │ │ Disk I/O Chart         │     │
│  │ Line Chart             │ │ Line Chart             │     │
│  └────────────────────────┘ └────────────────────────┘     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**关键功能**：
- 请求事件列表和筛选
- 点击查看详细追踪
- 系统资源使用趋势

---

### 内容列表 (PagesPage)

**功能定位**：跨知识库的内容（页面）统一管理

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 标题：内容列表                                            │
│  - 时间范围选择：24h | 7d | 30d                              │
├─────────────────────────────────────────────────────────────┤
│  Filter Section                                             │
│  知识库ID | 数据源ID | 已索引 | HTTP状态 | 搜索(URL/标题)    │
│  [ 刷新 ] [ 清空 ]                                          │
│  Filter Chips                                               │
├─────────────────────────────────────────────────────────────┤
│  Content Table                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ID | 知识库 | 标题 | URL | HTTP | 已索引 | 最近抓取 | 操作 │   │
│  │ 123 | kb_xx | 产品介绍 | https://... | 200 | ✓ | ... | ▶ │   │
│  └─────────────────────────────────────────────────────┘   │
│  Pagination                                                 │
└─────────────────────────────────────────────────────────────┘
```

**状态指示**：
- HTTP 状态码（>=400 标红）
- 已索引状态（Badge）
- 最近抓取时间

**关键操作**：
- 筛选和搜索
- 触发重抓取
- 进入详情

---

### 内容详情 (PageDetailPage)

**功能定位**：单个页面的完整信息和管理

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 面包屑：内容 > page_123                                   │
│  - 操作：Recrawl | 删除                                      │
├─────────────────────────────────────────────────────────────┤
│  Basic Info Card                                            │
│  ID | KB | Source | URL | 标题 | HTTP状态 | 最近抓取时间     │
├─────────────────────────────────────────────────────────────┤
│  Chunks Stats Card                                          │
│  total | with_embedding | embedding_coverage                │
│  embedding_models (JSON View)                               │
├─────────────────────────────────────────────────────────────┤
│  Markdown Content Card                                      │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ <pre> 原始 Markdown 内容 </pre>                      │   │
│  │ (可滚动，最大高度限制)                                │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  Meta Card                                                  │
│  JSON View 展示元数据                                        │
└─────────────────────────────────────────────────────────────┘
```

**关键操作**：
- Recrawl（触发重新抓取）
- 删除（带确认对话框）
- 查看关联任务

---

### 任务中心 (JobsPage)

**功能定位**：统一的任务（抓取/索引）管理和监控

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 标题：任务中心                                            │
│  - 快捷操作：只看失败 | 刷新                                 │
├─────────────────────────────────────────────────────────────┤
│  Filter Section                                             │
│  知识库ID | 应用ID | 数据源ID | 类型 | 状态 | 搜索(job_id)   │
│  [ 刷新 ] [ 清空 ]                                          │
│  Filter Chips                                               │
├─────────────────────────────────────────────────────────────┤
│  Job List Table                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 任务ID | 类型 | 状态 | 知识库 | 数据源 | 开始 | 结束 | 操作 │   │
│  │ job_xx | 抓取 | 运行中 | kb_xx | src_xx | ... | - | ▶   │   │
│  │ job_yy | 索引 | 失败   | kb_xx | -      | ... | ... | ▶ │   │
│  └─────────────────────────────────────────────────────┘   │
│  Pagination                                                 │
└─────────────────────────────────────────────────────────────┘
```

**任务类型**：
- `crawl`: 抓取任务
- `index`: 索引任务

**任务状态**：
- `queued`: 排队中
- `running`: 运行中
- `succeeded`: 成功
- `failed`: 失败

**关键操作**：
- 查看详情
- 重试（失败任务）
- 取消（排队任务）

---

### 任务详情 (JobDetailPage)

**功能定位**：单个任务的详细信息和日志

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 面包屑：任务 > job_xxx                                    │
│  - 状态 Badge                                               │
│  - 操作：重试 | 取消                                         │
├─────────────────────────────────────────────────────────────┤
│  Basic Info Card                                            │
│  ID | 类型 | 状态 | 知识库 | 数据源 | 开始时间 | 结束时间     │
├─────────────────────────────────────────────────────────────┤
│  Progress Card                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Progress Bar (if running)                            │   │
│  │ ████████████░░░░░░░░ 60%                            │   │
│  │ processed: 60 / total: 100                          │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  Error Card (if failed)                                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 错误信息展示                                         │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  Subtasks Card (if applicable)                              │
│  子任务列表                                                  │
├─────────────────────────────────────────────────────────────┤
│  Logs Card                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 实时日志输出                                         │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

### 审计日志 (AuditPage)

**功能定位**：系统操作的审计追踪

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 标题：审计日志                                            │
├─────────────────────────────────────────────────────────────┤
│  Filter Section                                             │
│  操作者 | 操作类型 | 对象类型 | 时间范围 | 搜索              │
├─────────────────────────────────────────────────────────────┤
│  Audit Log Table                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 时间 | 操作者 | 操作 | 对象类型 | 对象ID | 详情       │   │
│  │ ... | admin | create | kb | kb_xxx | {...}          │   │
│  │ ... | admin | delete | page | 123 | {...}           │   │
│  └─────────────────────────────────────────────────────┘   │
│  Pagination                                                 │
└─────────────────────────────────────────────────────────────┘
```

**记录内容**：
- 操作者
- 操作类型（create, update, delete, etc.）
- 对象类型（kb, app, page, source, etc.）
- 对象 ID
- 详情（JSON）
- 时间戳

---

### 系统设置 (SettingsPage)

**功能定位**：系统级配置和健康检查

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 标题：系统设置                                            │
├─────────────────────────────────────────────────────────────┤
│  Health Check Card                                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 服务 | 状态 | 延迟                                    │   │
│  │ API Server | ✓ Healthy | 5ms                        │   │
│  │ Database | ✓ Healthy | 10ms                         │   │
│  │ Vector Store | ✓ Healthy | 15ms                     │   │
│  │ [刷新]                                               │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  Model Configs Card                                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ LLM Models                                           │   │
│  │ - gpt-4 (default)                                    │   │
│  │ - gpt-3.5-turbo                                      │   │
│  │                                                      │   │
│  │ Embedding Models                                     │   │
│  │ - text-embedding-ada-002 (default)                   │   │
│  │                                                      │   │
│  │ Rerank Models                                        │   │
│  │ - bge-reranker-large                                 │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  Model Test Card                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 选择模型: [dropdown]                                  │   │
│  │ 输入测试文本: [input]                                │   │
│  │ [测试]                                               │   │
│  │ 结果: ...                                            │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

### 检索事件详情 (RetrievalEventDetailPage)

**功能定位**：单次检索请求的完整追踪信息

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────┐
│  Header Section                                             │
│  - 面包屑：可观测性 > evt_xxx                                │
│  - 状态 Badge                                               │
├─────────────────────────────────────────────────────────────┤
│  Overview Card                                              │
│  Event ID | 应用 | 状态 | 总延迟 | 时间                      │
├─────────────────────────────────────────────────────────────┤
│  Timing Breakdown Card                                      │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 阶段 | 耗时                                          │   │
│  │ Embedding | 100ms                                    │   │
│  │ Vector Search | 50ms                                 │   │
│  │ Rerank | 200ms                                       │   │
│  │ LLM Generation | 800ms                               │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  Query & Response Card                                      │
│  Query: 用户问题                                            │
│  Response: 生成的回答                                        │
├─────────────────────────────────────────────────────────────┤
│  Retrieved Sources Card                                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Source 1: [score: 0.95] 标题 | URL                   │   │
│  │ Source 2: [score: 0.88] 标题 | URL                   │   │
│  │ ...                                                  │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  Token Usage Card                                           │
│  Prompt Tokens | Completion Tokens | Total Tokens           │
└─────────────────────────────────────────────────────────────┘
```

---

## 用户流程图

### 核心用户旅程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         RAG Admin 核心用户旅程                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. 创建知识库                                                          │
│     登录 → 仪表盘 → 知识库列表 → 新建 → 填写配置 → 创建成功              │
│                                                                         │
│  2. 配置数据源                                                          │
│     知识库详情 → 数据源 Tab → 添加数据源 → 选择类型 → 配置 URL → 保存   │
│                                                                         │
│  3. 触发抓取                                                            │
│     数据源列表 → 触发抓取 → 跳转任务详情 → 监控进度 → 完成              │
│                                                                         │
│  4. 创建应用                                                            │
│     应用列表 → 新建 → 填写配置 → 关联知识库 → 创建成功                   │
│                                                                         │
│  5. 测试应用                                                            │
│     应用详情 → 测试 Tab → 输入问题 → 查看回答 → 验证效果                │
│                                                                         │
│  6. 监控质量                                                            │
│     质量监控 → 查看指标 → 发现问题 → 查看反馈 → 优化配置                │
│                                                                         │
│  7. 排障调试                                                            │
│     可观测性 → 搜索请求 → 查看详情 → 分析延迟/错误 → 定位问题           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 页面导航关系

```
                                    ┌─────────────┐
                                    │   登录页    │
                                    └──────┬──────┘
                                           │
                                           ▼
                                    ┌─────────────┐
                              ┌─────│   仪表盘    │─────┐
                              │     └──────┬──────┘     │
                              │            │            │
              ┌───────────────┼────────────┼────────────┼───────────────┐
              │               │            │            │               │
              ▼               ▼            ▼            ▼               ▼
       ┌────────────┐  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐
       │ 知识库列表  │  │  应用列表  │ │  用户反馈  │ │  质量监控  │ │  系统设置  │
       └─────┬──────┘  └─────┬──────┘ └────────────┘ └────────────┘ └────────────┘
             │               │
             ▼               ▼
       ┌────────────┐  ┌────────────┐
       │ 知识库详情  │  │  应用详情  │
       └─────┬──────┘  └────────────┘
             │
    ┌────────┼────────┐
    │        │        │
    ▼        ▼        ▼
┌────────┐ ┌────────┐ ┌────────┐
│内容列表 │ │任务列表 │ │  审计  │
└───┬────┘ └───┬────┘ └────────┘
    │          │
    ▼          ▼
┌────────┐ ┌────────┐
│内容详情 │ │任务详情 │
└────────┘ └────────┘
```

---

## 设计改进建议

### 1. 信息架构优化

**当前问题**：
- 侧边栏项目较多，分组逻辑不够直观
- 部分页面功能重叠（如内容列表和知识库详情中的内容 Tab）

**建议**：
- 简化导航为 3-4 个顶级入口
- 考虑使用二级导航或面包屑导航
- 合并功能重叠的页面

### 2. 一致性提升

**当前问题**：
- 不同页面的筛选器布局不完全一致
- 表格操作列的按钮风格略有差异

**建议**：
- 统一筛选器组件的设计规范
- 定义统一的表格操作模式

### 3. 可视化增强

**当前问题**：
- 部分页面的数据可视化较简单
- 状态展示主要依赖文字和 Badge

**建议**：
- 添加更多图表和可视化组件
- 使用图标增强状态识别
- 考虑添加实时更新功能

### 4. 移动端适配

**当前问题**：
- 当前设计偏重桌面端
- 表格在小屏幕上可能需要横向滚动

**建议**：
- 设计响应式布局
- 考虑关键功能的移动端优化

### 5. 用户引导

**当前问题**：
- 新用户可能不清楚操作流程
- 缺少空状态的操作引导

**建议**：
- 添加新手引导流程
- 优化空状态页面，提供明确的下一步操作
- 考虑添加帮助文档链接

---

## 附录：组件库概览

### 通用组件

| 组件 | 用途 |
|------|------|
| `Card` | 内容容器，带标题和描述 |
| `Button` | 按钮（多种变体：default, outline, destructive） |
| `Badge` | 状态标签 |
| `Select` | 下拉选择 |
| `Input` | 输入框 |
| `DebouncedInput` | 防抖输入框（用于搜索） |
| `Table` | 数据表格 |
| `Pagination` | 分页器 |
| `Loading` | 加载状态 |
| `EmptyState` | 空状态 |
| `ApiErrorBanner` | 错误提示 |
| `ConfirmDangerDialog` | 危险操作确认对话框 |
| `FilterChips` | 筛选条件标签 |
| `CopyableText` | 可复制文本 |
| `JsonView` | JSON 数据展示 |
| `Tabs` | 选项卡 |

### 图表组件

| 组件 | 用途 |
|------|------|
| `AreaChart` | 面积图 |
| `BarChart` | 柱状图 |
| `LineChart` | 折线图 |

---

*文档生成时间：2026-01-22*
*用途：AI 页面重新设计参考*
